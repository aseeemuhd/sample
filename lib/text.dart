import 'package:flutter/material.dart';

class textpage extends StatelessWidget {
textpage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('loginpage',style: TextStyle(color: Colors.black),)),
    );
  }
}